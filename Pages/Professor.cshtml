@page
@model ProfessorModel
@{
    ViewData["Title"] = "Professor Page";
}

<h3>Professors</h3>
<hr />

<label class="control-label"></label>
<p>Select a Professor</p>
<form method = "post">
    <select asp-items="Model.ProfessorDropDown" class="form-control">
        <option value = "">-- Select Professor --</option>
    </select>
</form>

<p>You have selected {@Model.ProfessorDropDown}.</p>
@{
    if(Professor != null)
    {
        <p>This professor teaches:</p>
        foreach(var c in Model.Courses)
        {
            <p>@c.Description</p>
        }
    }
    if (Course.Description == null)
    {
        @:This professor isn't teaching an courses.
    }
    else 
    {
        
    } 
}